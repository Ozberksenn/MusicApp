import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    backgroundColor: "#2e3034",
  },
  title: {
    color: "#fff",
    textAlign: "center",
    fontSize: 22,
  },
});
