import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    height: 100,
    margin: 10,
    borderWidth: 1,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 5,
  },
  name: {
    color: "#fff",
    fontSize: 24,
    textAlign: "center",
  },
});
