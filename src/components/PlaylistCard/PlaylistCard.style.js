import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginLeft: 10,
    marginRight: 20,
    marginTop: 10,
    width: 180,
    height: 100,
    resizeMode: "contain",
    opacity: 0.7,
    justifyContent: "center",
    alignItems: "center",
  },
  name: {
    fontSize: 18,
    fontWeight: "bold",
    backgroundColor: "#fff",
  },
});
